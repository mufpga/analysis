String ch1;

volt = 0.9;
channel = 0;
f = new FileOutputStream("AI"+channel+"_"+volt+".txt", true);
p = new PrintStream(f); 
this.interpreter.setOut(p); 

for(int i=0;i<100;i++){
	ch1 = mmc.getProperty("AnalogInput","AnalogInput"+channel);
	print(ch1);
	mmc.sleep(10);
}

f.close();